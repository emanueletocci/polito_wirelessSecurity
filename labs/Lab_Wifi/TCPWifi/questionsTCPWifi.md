Based on the provided `iperf` logs, here is the analysis and comparison of the sender and receiver TCP test files to answer your questions.

### 1) Why are the results different?
* **Wireless Channel Variability:** The tests were conducted over a Wi-Fi network, which relies on a shared and unguided radio medium. [cite_start]Across the 10 separate tests, the overall throughput varies greatly; for instance, the sender recorded an average of 51.8 Mbits/sec in Test 1 [cite: 18][cite_start], but achieved 72.6 Mbits/sec in Test 2[cite: 20]. 
* **Environmental Factors:** In a wireless environment, differences between successive tests are caused by changing conditions such as local interference, signal fading, and the CSMA/CA (Carrier-Sense Multiple Access with Collision Avoidance) protocol managing collisions on the fly. 

### 2) Why did the goodput change during the 10s of the experiment?
* **TCP Congestion Control:** Within any single 10-second run, TCP continuously probes the network for maximum available capacity using an algorithm called AIMD (Additive Increase, Multiplicative Decrease). 
* **Reaction to Wireless Errors:** If the wireless channel experiences sudden interference or MAC-layer retries fail, packets are delayed or lost. TCP misinterprets this as network congestion and dramatically shrinks its transmission window. [cite_start]This behavior is highly visible in the sender's Test 4, where the bandwidth sharply drops from 80.4 Mbits/sec down to 35.3 Mbits/sec, and then plummets to 13.5 Mbits/sec in the following second[cite: 22].

### 3) Why are the receiver and the sender measurements different? Which is the correct goodput estimation?
* **Measurement Perspective:** The sender's application simply measures how fast it can push data into the local operating system's transmission buffer (socket). Conversely, the receiver measures the rate at which data is successfully transmitted across the network, acknowledged, and delivered to the receiving application.
* **The Correct Estimation:** The **receiver** measurement is the correct goodput estimation. Goodput is strictly defined as the actual application-level throughput—meaning only the usable data that successfully arrives at the final destination counts. 

### 4) What changes if we invert the sender and the receiver role?
* **Asymmetric TCP Buffers:** The logs reveal a significant configuration difference between the two hosts. [cite_start]The sender machine (`172.21.139.7`) operates with a default TCP window size of only 16.0 KByte[cite: 17]. [cite_start]The receiver machine (`172.21.139.32`) has a much larger default window size of 128 KByte[cite: 1]. If you invert the roles, the new receiver will be bottlenecked by its tiny 16.0 KByte receive window, heavily restricting the amount of data TCP allows in flight and likely crippling the overall throughput.
* **Physical Link Asymmetry:** Wi-Fi is rarely perfectly symmetrical. Reversing the flow flips the traffic from uplink to downlink (or vice-versa). The two devices likely have unequal transmission power limits, different antenna designs, and experience different localized noise, meaning the physical capacity of the wireless channel will fundamentally change depending on which device is transmitting.